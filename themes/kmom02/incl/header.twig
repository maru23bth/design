<!DOCTYPE html>
<html class="no-js" lang="sv">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{% if meta.title %}{{ meta.title }} | {% endif %}{{ site_title }}</title>

    {% if meta.description %}
    <meta name="description" content="{{ meta.description|striptags }}" />
    {% endif %}

    {% if current_page %}
    <link rel="canonical" href="{{ current_page.url }}" />
    {% endif %}

    <link rel="icon" href="{{ theme_url }}/../shared/img/favicon.ico">
    <!--link rel="preload" href="https://fonts.googleapis.com/css2?family=Roboto:wght@100;300;400;500;700;900&display=swap" as="style" onload="this.onload = null;this.rel = 'stylesheet'"-->
    <!--link rel="stylesheet" href="{{ theme_url }}/css/style.css" type="text/css" /-->
    {% if config.session.theme == "dark" %}
        <!--link rel="stylesheet" href="{{ theme_url }}/css/style-dark.min.css" type="text/css" /-->
        <link rel="preload" href="{{ theme_url }}/css/style-dark.min.css" as="style" onload="this.onload = null;this.rel = 'stylesheet'" />
    {% else %}
        <!--link rel="stylesheet" href="{{ theme_url }}/css/style.min.css" type="text/css" /-->
        <link rel="preload" href="{{ theme_url }}/css/style.min.css" as="style" onload="this.onload = null;this.rel = 'stylesheet'" />
    {% endif %}

</head>
